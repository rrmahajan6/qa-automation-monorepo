# Test Strategy

- Smoke tests run on pull requests.
- Full regression runs on schedule or manual trigger.
- Keep API and UI tests separated for faster feedback.
- Publish reports per framework and aggregate under `shared/reports` when needed.
