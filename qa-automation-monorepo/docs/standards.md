# Standards

- Keep framework code isolated to its own folder.
- Avoid cross-language dependency coupling.
- Use environment variables for secrets and URLs.
- Keep test data in `shared/test-data` when reusable.
- Keep pipeline ownership and docs updated per framework.
